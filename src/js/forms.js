/* forms.js — quote / estimate / contact form helpers.
   Existing templates submit inline (`event.preventDefault()` + alert demo).
   This module exists for the future server integration (validation, fetch,
   honeypot, recaptcha). Currently a no-op shim.
*/
(() => {
  'use strict';
  // intentionally empty — wire up real submission handler in a future prompt
})();
